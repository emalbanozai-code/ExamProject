this is my project Gym MIS backend and frontend.
I want to make it in react ts + tailwind v4 + django drf.
well structured and modular.
in frontend each module should have services(api calls),hooks, stores, schemas, components, queries, pages and types folders.
each code should be in the appropriate folder.
I HAVE trainer module.
but i want to have staff module instead whcih contains trainers, clerk, manager and other staff members which have salary attendance and some other fields in common.
but trainer is a bit different it assigns to classes and have schedules.
so i need a staff model and a trainer model to get foreign key from staff module.
and later i will have a schedule module which controls assignment to classe.
so remove the assign class from trainer model.
make the appropriate changes in backend and frontend.
one app named staff in backend and one module named staff in frontend.
staff model should have a field to specify position of staff like clerk trainer ,  manager and etc.

in frontend when choose position of trainer frontend should show also trainer specific fields and when saving create a trainer record as well in backend.

